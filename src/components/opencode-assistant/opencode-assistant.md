# OpenCode Assistant Panel

This directory contains the OpenCode Assistant UI used to drive a live opencode serve session.
Each part type has its own renderer file so formatting can evolve independently.
Agent and model selectors are separate components to keep prompt configuration modular.
